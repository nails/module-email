<?php

/**
 * Slot: masthead, plain text part
 *
 * `{{appName}}` rather than the value itself, so that an application which
 * renames itself does not have to resend anything sitting in the queue.
 *
 * @var \stdClass|null $emailObject
 */

?>
{{appName}}
